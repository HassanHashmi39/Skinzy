services:
  - type: web
    name: skinzy-backend
    env: node
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 10000
      - key: NODE_ENV
        value: production
      # Note: MONGO_URI and JWT_SECRET must be set manually in the Render dashboard
